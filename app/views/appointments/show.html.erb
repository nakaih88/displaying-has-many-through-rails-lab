<h1><%= @appt.appointment_datetime.strftime("%B %e, %Y at %H:%M") %></h1>
<p>Doctor: <%= link_to @appt.doctor.name, doctor_path(@appt.doctor) %></p>
Patient Name: <%= link_to @appt.patient.name, patient_path(@appt.patient) %>